/**
 * Generate a 5-star rating string with optional half-star.
 * @param n A number between 0 and 5 (inclusive).
 * @returns A string like "★★★⯪☆"
 */
declare function rating(n: number): string;
export = rating;
